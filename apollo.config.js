module.exports = {
  client: {
    service: {
      name: "lighthouse-repro-api",
      // URL to the GraphQL API
      url: "",
    },
    // Files processed by the extension
    includes: ["src/**/*.vue", "src/**/*.js"],
  },
};
